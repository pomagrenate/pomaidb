# PomaiDB Node SDK (beta target)

Node SDK is planned after Rust GA, with parity for key ingest/search workflows.

Planned features:
- N-API bindings over C ABI
- Promise-based gateway client
- conformance tests against shared fixtures
